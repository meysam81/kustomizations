---
configMapGenerator:
  - files:
      - config.yml=oathkeeper-server-config.yml
    name: oathkeeper-config

resources:
  - ../../base
  - vmservicescrape.yml

images:
  - name: oryd/oathkeeper
    newTag: v0.40.7-distroless
  - name: bitnami/kubectl
    newTag: 1.31.3

patches:
  - patch: patch-deployment.yml
    target:
      kind: Deployment
      name: oathkeeper

namespace: auth
